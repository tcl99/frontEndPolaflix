import { Outlet } from "react-router-dom";
import { SeriesAPI } from "../api/SeriesAPI";

interface Props {
  s: string;
}

const Serie = () => {
  return (
    <>
      <h1>{"a"}</h1>
    </>
  );
};

export default Serie;
